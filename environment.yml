# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: catastrophy-pipeline
channels:
  - conda-forge
  - bioconda
  - defaults
  - darcyabjones
dependencies:
  - bioconda::hmmer=3.2.1
  - darcyabjones::catastrophy=0.0.2
  - wget
  - gawk
